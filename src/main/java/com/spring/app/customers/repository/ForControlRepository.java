package com.spring.app.customers.repository;

import com.spring.app.customers.models.ForControl;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ForControlRepository extends JpaRepository<ForControl, Integer> {
}
